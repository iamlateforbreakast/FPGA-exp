(executable
 (public_name spi_hardcaml)
 (name generate)
 (modules generate)
 (libraries project08_lib hardcaml)
 (preprocess (pps ppx_jane ppx_hardcaml)))

 (executable
  (name simulate)
  (modules simulate)
  (libraries project08_lib hardcaml hardcaml_waveterm)
  (preprocess (pps ppx_jane ppx_hardcaml)))
