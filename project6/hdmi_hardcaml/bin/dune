(executable
 (public_name hdmi_hardcaml)
 (name main)
 (modules main)
 (libraries project6_lib hardcaml)
 (preprocess (pps ppx_jane ppx_hardcaml)))

 (executable
  (name simulation)
  (modules simulation)
  (libraries project6_lib hardcaml hardcaml_waveterm)
  (preprocess (pps ppx_jane ppx_hardcaml)))


